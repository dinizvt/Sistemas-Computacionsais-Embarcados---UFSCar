#ifndef BITMAPS_H
#define BITMAPS_H

// Store bitmaps in code memory to save RAM
// Player Car (14x16) - Optimized size
extern unsigned char car_bitmap[28];

// Obstacle Car (14x8) - Single page
extern unsigned char obstacle_bitmap[14];

#endif

